{
  "name": "ai-shared-stratum",
  "private": true,
  "scripts": {
    "build:html": "tsx scripts/build-html.ts",
    "build:graph": "tsx scripts/build-graph.ts",
    "build:graph3d": "tsx scripts/build-graph-3d.ts",
    "build:stats": "tsx scripts/document-stats.ts",
    "apply:definition-labels": "tsx scripts/apply-definition-labels.ts",
    "apply:section-refs": "tsx scripts/apply-section-refs.ts",
    "render:pipeline": "pnpm run apply:section-refs && pnpm run apply:definition-labels && pnpm run build:html && pnpm run build:graph && pnpm run build:graph3d",
    "list:section-refs": "tsx scripts/list-section-refs.ts",
    "validate:schema": "tsx scripts/validate.ts"
  },
  "devDependencies": {
    "@types/node": "^25.0.3",
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3"
  },
  "dependencies": {
    "schemas-lib-vera-markdown": "file:vera-markdown-2.0.0.tgz",
    "zod": "^3.25.76"
  }
}
